server:
  port: 80
  session-timeout: 30
  tomcat.max-threads: 0
  tomcat.uri-encoding: UTF-8
spring:
  mvc:
    view:
      prefix: classpath:/templates/,classpath:/static/
      suffix: .html
  freemarker:
    cache: false
  datasource:
    url: jdbc:oracle:thin:@127.0.0.1:1521:orcl
    username: zndx
    password: zndx
    driver-class-name: oracle.jdbc.driver.OracleDriver
  redis:
    database: 0
    host: localhost
    port: 6379
    timeout: 2000s
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
    lettuce:
      shutdown-timeout: 100ms
logging:
  level:
    com.sh.springbootdemo.controller: debug
mybatis:
  mapper-locations: classpath:mapper/*.xml
  config-locations: classpath:mybatis/mybatis-config.xml
  type-aliases-package: com.sh.springbootdemo.bean
user:
  id: 01
  name: 张
  sex: 男
  money: 10
  grade: 1